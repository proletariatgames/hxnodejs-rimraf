# Haxe/hxnodejs externs for the [rimraf](https://www.npmjs.com/package/rimraf) npm library

Tested using `rimraf` version **2.5.2**

Example:
```haxe
import js.npm.Rimraf;

Rimraf.rimraf(path, function(err) { });
```
